# Section nav at boundary: cycling backward into Section mode lands on the
# only section (the heading at node 0); j attempts to advance and hits the
# section boundary (silent — no other section to wrap to). Final anchor is
# the section start.
Backspace
Backspace
Backspace
j
q
